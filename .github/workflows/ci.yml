name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore dependencies
        run: dotnet restore PodcastMetadataGenerator.sln

      - name: Build Solution
        run: dotnet build PodcastMetadataGenerator.sln --no-restore --configuration Release

      - name: Test
        run: dotnet test PodcastMetadataGenerator.sln --no-build --configuration Release --verbosity normal
        continue-on-error: true # No tests yet

  pack:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Create NuGet package (Console tool)
        run: dotnet pack src/Console/PodcastMetadataGenerator.Console.csproj --configuration Release --output ./artifacts

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: nuget-package
          path: ./artifacts/*.nupkg
